CREATE TABLE uta_eq (
   id INTEGER ,
   outcome_id INTEGER,
   data_type VARCHAR2(100),
   check_value_id INTEGER,
   against_value_id INTEGER,
  CONSTRAINT uta_eq_pk PRIMARY KEY (id)
);

ALTER TABLE uta_eq ADD CONSTRAINT uta_eq_outcome_fk
   FOREIGN KEY (outcome_id) REFERENCES ut_outcome;
   
ALTER TABLE uta_eq ADD CONSTRAINT uta_eq_check_fk
   FOREIGN KEY (check_value_id) REFERENCES utv_value;   

ALTER TABLE uta_eq ADD CONSTRAINT uta_eq_against_fk
   FOREIGN KEY (against_value_id) REFERENCES utv_value; 
